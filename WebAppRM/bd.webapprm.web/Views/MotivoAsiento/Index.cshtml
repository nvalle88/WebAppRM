@model List<bd.webapprm.entidades.MotivoAsiento>

@{
    ViewData["Title"] = "Motivo de Asiento";
}

<div id="content">
    <p>
        <a asp-action="Create" class="btn btn-success">Nuevo Motivo Asiento</a>
    </p>

    <section id="widget-grid" class="">
        <div class="row">
            <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <div class="jarviswidget jarviswidget-color-darken" id="wid-id-0" data-widget-editbutton="false">
                    <header>
                        <span class="widget-icon"> <i class="fa fa-table"></i> </span>
                        <h2>Motivos de Asiento</h2>
                    </header>
                    <div>
                        <div class="jarviswidget-editbox"></div>
                        <div class="widget-body no-padding">
                            <table id="dt_basic" class="table table-striped table-bordered table-hover" style="width:100%;">
                                <thead>
                                    <tr>
                                        <th>
                                            Descripci&oacute;n
                                        </th>
                                        <th>
                                            Configuraci&oacute;n Contabilidad
                                        </th>                                        
                                        <th>Opciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Descripcion)
                                            </td>

                                            <td>
                                                @Html.DisplayFor(modelItem => item.ConfiguracionContabilidad.idConfiguracionContabilidad)
                                            </td>
                                            <td>
                                                @Html.ActionLink("Editar", "Edit", new { id = item.IdMotivoAsiento }) |
                                                @Html.ActionLink("Eliminar", "Delete", new { id = item.IdMotivoAsiento }, new { onclick = "return confirm('Desea eliminar el Motivo de Asiento... ?');" })
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </article>
        </div>
    </section>
</div>
@section Scripts {
    <script src='~/js/datatables/init.js'></script>
}
