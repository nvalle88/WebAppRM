@model List<bd.webapprm.entidades.MotivoTransferencia>

<div id="content">
    <p>
        <a asp-action="Create" class="btn btn-success">Nuevo Motivo de Transferencia</a>
    </p>
    <section id="widget-grid" class="">
        <div class="row">
            <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <div class="jarviswidget jarviswidget-color-darken" id="wid-id-0" data-widget-editbutton="false">
                    <header>
                        <span class="widget-icon"> <i class="fa fa-table"></i> </span>
                        <h2>Listado de Motivos de Transferencias</h2>
                    </header>
                    <div>
                        <div class="jarviswidget-editbox">
                        </div>
                        <div class="widget-body no-padding">
                            <table id="dt_basic" class="table table-striped table-bordered table-hover" width="100%">
                                <thead>
                                    <tr>
                                        <th class="hidden">
                                            @Html.DisplayNameFor(model => model.FirstOrDefault().IdMotivoTransferencia)
                                        </th>
                                        <th>
                                            @Html.DisplayNameFor(model => model.FirstOrDefault().Motivo_Transferencia)
                                        </th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td class="hidden">
                                                @Html.DisplayFor(modelItem => item.IdMotivoTransferencia)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Motivo_Transferencia)
                                            </td>
                                            <td>
                                                @Html.ActionLink("Editar", "Edit", new { id = item.IdMotivoTransferencia }) |
                                                <a href="javascript:void(0);" id="@item.IdMotivoTransferencia" onclick="abrirVentanaConfirmacion('@item.IdMotivoTransferencia')" data-url="@Url.Action("Delete", values: new { id = item.IdMotivoTransferencia })" data-titulo="Eliminar" data-descripcion="&#191;Desea eliminar el Motivo de Transferencia... ?">Eliminar</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </article>
        </div>
    </section>
</div>

@section Scripts {
    <script src='~/js/datatables/init.js'></script>
}


