@model List<bd.webapprm.entidades.CatalogoCuenta>

<div id="content">
    <p>
        <a asp-action="Create" class="btn btn-success">Nuevo Cat&aacute;logo de Cuenta</a>
    </p>
    <section id="widget-grid" class="">
        <div class="row">
            <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <div class="jarviswidget jarviswidget-color-darken" id="wid-id-0" data-widget-editbutton="false">
                    <header>
                        <span class="widget-icon"> <i class="fa fa-table"></i> </span>
                        <h2>Cat&aacute;logos de Cuenta</h2>
                    </header>
                    <div>
                        <div class="jarviswidget-editbox"></div>
                        <div class="widget-body no-padding">
                            <table id="dt_basic" class="table table-striped table-bordered table-hover" style="width:100%;">
                                <thead>
                                    <tr>
                                        <th>
                                            C&oacute;digo
                                        </th>
                                        <th>
                                            Cat&aacute;logo Hijo
                                        </th>
                                        <th>Opciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Codigo)
                                            </td>

                                            <td>
                                                @Html.DisplayFor(modelItem => item.CatalogoCuentaHijo.Codigo)
                                            </td>

                                            <td>
                                                @Html.ActionLink("Editar", "Edit", new { id = item.IdCatalogoCuenta }) |
                                                <a href="javascript:void(0);" id="@item.IdCatalogoCuenta" onclick="abrirVentanaConfirmacion('@item.IdCatalogoCuenta')" data-url="@Url.Action("Delete", values: new { id = item.IdCatalogoCuenta })" data-titulo="Eliminar" data-descripcion="&#191;Desea eliminar el Cat&aacute;logo de Cuenta... ?">Eliminar</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </article>
        </div>
    </section>
</div>
@section Scripts {
    <script src='~/js/datatables/init.min.js'></script>
}
