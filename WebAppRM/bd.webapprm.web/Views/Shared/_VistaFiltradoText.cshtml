@model int
@{ 
    var listaCustomFilter = ViewBag.ListadoCustomFilter as List<PropiedadValor> ?? new List<PropiedadValor>();
    bool mostrarFiltradoUltimaColumna = ViewBag.MostrarFiltradoUltimaColumna ?? false;
}
<tr>
    @{
        for (int i = 0; i < Model; i++)
        {
            if (i == (Model - 1) && !mostrarFiltradoUltimaColumna)
            {
                <th class="hasinput"></th>
            }
            else
            {
                <th class="hasinput">
                    <input type="text" class="form-control" placeholder="Filtrar..." />
                </th>
            }
        }
    }
</tr>