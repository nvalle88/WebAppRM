@model List<bd.webapprm.entidades.TablaDepreciacion>
@{
    ViewData["Title"] = "Index";
}

<div id="content">
    <p>
        <a asp-action="Create" class="btn btn-success"> Nuevo Tabla Depreciación</a>
    </p>
    <section id="widget-grid" class="">
        <div class="row">
            <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <div class="jarviswidget jarviswidget-color-darken" id="wid-id-0" data-widget-editbutton="false">
                    <header>
                        <span class="widget-icon"> <i class="fa fa-table"></i> </span>
                        <h2>Tabla Depreciación</h2>
                    </header>
                    <div>
                        <div class="jarviswidget-editbox"></div>
                        <div class="widget-body no-padding">
                            <table id="dt_basic" class="table table-striped table-bordered table-hover" style="width:100%;">
                                <thead>
                                    <tr>
                                        <th>
                                            Indice Depreciacion
                                        </th>
                                        
                                        <th>Opciones</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.IndiceDepreciacion)
                                            </td>

                                            <td>
                                                @Html.ActionLink("Editar", "Edit", new { id = item.IdTablaDepreciacion }) |
                                                <a href="javascript:void(0);" id="@item.IdTablaDepreciacion" onclick="abrirVentanaConfirmacion('@item.IdTablaDepreciacion')" data-url="@Url.Action("Delete", values: new { id = item.IdTablaDepreciacion })" data-titulo="Eliminar" data-descripcion="&#191;Desea eliminar la Tabla Depreciaci&oacute;n... ?">Eliminar</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </article>
        </div>
    </section>
</div>
@section Scripts {
   <script src='~/js/datatables/init.js'></script>
}