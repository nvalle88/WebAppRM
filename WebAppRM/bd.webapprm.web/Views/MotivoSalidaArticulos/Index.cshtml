@model List<bd.webapprm.entidades.MotivoSalidaArticulos>

<div id="content">
    <p>
        <a asp-action="Create" class="btn btn-primary">Nuevo Motivo de Salida de Art&iacute;culo</a>
    </p>
    <section id="widget-grid" class="">
        <div class="row">
            <article class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                <div class="jarviswidget jarviswidget-color-darken" id="wid-id-0" data-widget-editbutton="false">
                    <header>
                        <span class="widget-icon"> <i class="fa fa-table"></i> </span>
                        <h2>Listado de Motivos de Salida de Art&iacute;culo</h2>
                    </header>
                    <div>
                        <div class="jarviswidget-editbox">
                        </div>
                        <div class="widget-body no-padding">
                            <table id="dt_basic" class="table table-striped table-bordered table-hover">
                                <thead>
                                    <tr>
                                        <th>
                                            @Html.DisplayNameFor(model => model.FirstOrDefault().Descripcion)
                                        </th>
                                        <th></th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.Descripcion)
                                            </td>
                                            <td>
                                                @Html.ActionLink("Editar", "Edit", new { id = item.IdMotivoSalidaArticulos }) |
                                                <a href="javascript:void(0);" id="@item.IdMotivoSalidaArticulos" onclick="abrirVentanaConfirmacion('@item.IdMotivoSalidaArticulos')" data-url="@Url.Action("Delete", values: new { id = item.IdMotivoSalidaArticulos })" data-titulo="Eliminar" data-descripcion="&#191;Desea eliminar el Motivo de salida de art&iacute;culo... ?">Eliminar</a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </article>
        </div>
    </section>
</div>

@section Scripts {
    <script src='~/js/datatables/init.min.js'></script>
}
